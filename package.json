{
  "name": "playwright-script_in_index_ts_runs_too_late",
  "version": "0.0.0",
  "scripts": {
    "install-project": "npm ci && npx playwright install",
    "playwright": "playwright test --config playwright.config.ts",
    "playwright-debug": "cross-env PWDEBUG=1 playwright test --config playwright.config.ts --debug"
  },
  "dependencies": {
    "react": "18.2.0",
    "react-dom": "18.2.0"
  },
  "devDependencies": {
    "@playwright/experimental-ct-react": "1.26.0",
    "@types/react": "18.0.20",
    "@types/react-dom": "18.0.6",
    "cross-env": "7.0.3",
    "typescript": "4.8.3"
  }
}
